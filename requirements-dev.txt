# Lightweight test/lint environment: unit + integration tests use fakes, so the
# heavy model stack (torch, fastembed, google-genai) is NOT required here.
# For a full local run, install requirements.txt as well.
fastapi>=0.115
pydantic>=2.7
pydantic-settings>=2.3
qdrant-client>=1.11
httpx>=0.27
PyJWT>=2.8
pypdf>=4.2
python-docx>=1.1
python-multipart>=0.0.9
pytest>=8.0
ruff>=0.5
